// let rama = new Promise(function(resolve, reject) {
//   setTimeout(() => resolve("done!"), 3000);
// });

// // resolve runs the first function in .then
// rama.then(
//   result => alert(result), // shows "done!" after 1 second
//   error => alert(error) // doesn't run
// );

